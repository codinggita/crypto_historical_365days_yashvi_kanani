{
  "name": "cryptoversex-workspace",
  "version": "1.0.0",
  "description": "CryptoVerseX — MERN Stack Crypto Analytics Platform Workspace",
  "private": true,
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx concurrently --kill-others-on-fail \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "lint": "npm run lint --prefix frontend",
    "build": "npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
